---
import TopNavigation from './TopNavigation';

import { getCollection } from 'astro:content';
const resourcesEntries = await getCollection('resources');
---

<TopNavigation resources={resourcesEntries} />
